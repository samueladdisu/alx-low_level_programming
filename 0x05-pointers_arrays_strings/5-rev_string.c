#include "main.h"
/**
 * rev_string - revesre a given string
 * @s: contains string
 * Returns: Always 0.
 */
void rev_string(char *s)
{
	int i;
	char ch[];

	for(i = 10; i >= 0; i--)
	{
		ch[] += s[i];
	}
	s[10] = ch[];
}
