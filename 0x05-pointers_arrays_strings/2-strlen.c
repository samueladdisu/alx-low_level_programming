#include "main.h"
/**
 * _strlen - return length of strings
 * @s: contains string
 * Return: length of the string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
